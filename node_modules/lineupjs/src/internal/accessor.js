import get from 'lodash.get';
export function resolveValue(row, column) {
    if (row.hasOwnProperty(column)) {
        return row[column];
    }
    return get(row, column);
}
//# sourceMappingURL=accessor.js.map