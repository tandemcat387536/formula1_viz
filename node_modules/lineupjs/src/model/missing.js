export function isMissingValue(v) {
    return typeof (v) === 'undefined' || v == null || (typeof v === 'number' && isNaN(v)) || v === '' || v === 'NA' || v === 'NaN' || (typeof (v) === 'string' && (v.toLowerCase() === 'na') || (Array.isArray(v) && v.every(function (v) { return isMissingValue(v); })));
}
export function isUnknown(v) {
    return v == null || v === undefined || isNaN(v);
}
export var FIRST_IS_NAN = -1;
export var FIRST_IS_MISSING = 1;
export var missingGroup = {
    name: 'Missing values',
    color: 'gray'
};
//# sourceMappingURL=missing.js.map