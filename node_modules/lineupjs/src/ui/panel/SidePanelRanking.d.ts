import Ranking from '../../model/Ranking';
import { IRankingHeaderContext } from '../interfaces';
import { ISidePanelOptions } from './SidePanel';
export default class SidePanelRanking {
    readonly ranking: Ranking;
    private ctx;
    private readonly options;
    readonly header: HTMLElement;
    readonly dropdown: HTMLElement;
    readonly node: HTMLElement;
    private readonly hierarchy;
    private readonly entries;
    constructor(ranking: Ranking, ctx: IRankingHeaderContext, document: Document, options: Readonly<ISidePanelOptions>);
    private init;
    active: boolean;
    update(ctx: IRankingHeaderContext): void;
    private updateHierarchy;
    private updateList;
    destroy(): void;
}
