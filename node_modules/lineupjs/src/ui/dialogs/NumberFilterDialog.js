import { __extends } from "tslib";
import ADialog from './ADialog';
var NumberFilterDialog = (function (_super) {
    __extends(NumberFilterDialog, _super);
    function NumberFilterDialog(column, dialog, ctx) {
        var _this = _super.call(this, dialog) || this;
        _this.column = column;
        _this.ctx = ctx;
        _this.summary = ctx.summaryRenderer(_this.column, true);
        return _this;
    }
    NumberFilterDialog.prototype.build = function (node) {
        node.classList.add('lu-dialog-mapper');
        node.insertAdjacentHTML('beforeend', this.summary.template);
        var summary = node.lastElementChild;
        summary.classList.add('lu-summary', 'lu-renderer');
        summary.dataset.renderer = this.column.getSummaryRenderer();
        summary.dataset.interactive = '';
        node.appendChild(summary);
        this.summary.update(this.node.querySelector('.lu-summary'), this.ctx.statsOf(this.column));
    };
    return NumberFilterDialog;
}(ADialog));
export default NumberFilterDialog;
//# sourceMappingURL=NumberFilterDialog.js.map